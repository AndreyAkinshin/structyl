{
  "name": "structyl-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-schemas": "cp ../schema/*.schema.json public/schema/",
    "dev": "pnpm run copy-schemas && vitepress dev",
    "prebuild": "pnpm run copy-schemas",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
